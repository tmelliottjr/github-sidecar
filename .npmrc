; The global npm config sets `replace-registry-host=always`, which rewrites all
; tarball URLs to packagefeedproxy.microsoft.io. That mirror 404s for some
; packages (e.g. @types/estree, a transitive dependency of rollup/vite), so we
; use the tarball hosts advertised by the registry metadata instead.
replace-registry-host=never
