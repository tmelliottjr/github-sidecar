/// <reference types="vite/client" />

/** Injected by vite.config.ts; only referenced by dev-only code. */
declare const __DEV_RELOAD_ORIGIN__: string

/** Injected by both Vite configs: the browser this bundle is built for. */
declare const __BROWSER__: 'chrome' | 'firefox' | 'safari'
